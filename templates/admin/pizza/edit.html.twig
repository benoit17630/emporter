{% extends 'base.html.twig' %}

{% block title %}modifier Pizza {% endblock %}

{% block body %}
    <div class="container stencil">
    <h1 class="text-center mt-4">modifier Pizza</h1>

    {{ include('admin/pizza/_form.html.twig', {'button_label': 'modifier'}) }}

    <a href="{{ path('admin_pizza_index') }}">retour a la liste</a>
    </div>

{% endblock %}
