{{ form_start(form) }}
{{ form_errors(form) }}
<div class="container">
    <div class="row police justify-content-center">
        <div class="col-8 col-md-3 mt-4">
            {{ form_label(form.name) }}
            {{ form_widget(form.name) }}
        </div>
        <div class="col-8 col-md-3 mt-4">
            {{ form_label(form.isFirst) }}
            {{ form_widget(form.isFirst) }}
        </div>
        <div class="col-8 col-md-3 mt-4">
            {{ form_label(form.isLast) }}
            {{ form_widget(form.isLast) }}
        </div>
        <div class="col-8 col-md-3 mt-4">
            {{ form_label(form.category) }}
            {{ form_widget(form.category) }}
        </div>
    </div>
    <div class="row justify-content-center my-4"><button class="btn btn-primary mb-3">{{ button_label|default('Enregistrer') }}</button></div>
    {{ form_end(form) }}

