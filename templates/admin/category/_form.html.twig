{{ form_start(form) }}
{{ form_errors(form) }}
<div class="container">
    <div class="row police justify-content-center">
        <div class="col-8 col-md-3 mt-4">
            {{ form_label(form.name) }}
            {{ form_widget(form.name) }}
        </div>



    </div>
    <div class="row justify-content-center my-4"><button class="btn btn-primary ">{{ button_label|default('Enregistrer') }}</button></div>
{{ form_end(form) }}