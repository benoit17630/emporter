{% extends 'base.html.twig' %}

{% block title %}Mon compte{% endblock %}

{% block body %}
    <h1 class="mt-5 text-center stencil">Mon compte</h1>
    <p class="stencil">Bienvenue  {{ app.user.firstname}} {{ app.user.lastname}}  dans votre compte. <br>
        C'est ici que vous allez pouvoir gérer vos information personnelles</p>

    <div class="row stencil mt-4">
        <div class="col-md-4">
            <div class="account-item text-center">
                <img src="{{ asset('assets/img/information.png') }}" alt="image d un cadenas" height="50px"> <br>
                <a href="{{ path('account_information') }}">Modifier mes information</a>
            </div>
        </div>
        <div class="col-md-4">
            <div class="account-item text-center">
                <img src="{{ asset('assets/img/password.png') }}" alt="image d un cadenas" height="50px"> <br>
                <a href="{{ path('account_password') }}">Modifier mon mot de passe</a>
            </div>
        </div>

        <div class="col-md-4">
            <div class="account-item text-center">
                <img src="{{ asset('assets/img/shopping.png') }}" alt="" height="50px"><br>
                <a href="{{ path('account_order') }}">Mes commandes</a>
            </div>
        </div>

    </div>
{% endblock %}
